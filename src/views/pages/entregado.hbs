<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=}, initial-scale=1.0">
    <title>Pedidos Entregados</title>
</head>

<body>
    <center class="mt-4">
        <h1>Pedidos Entregados</h1>
    </center>

    <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
      <div class="bg-white  shadow-xl sm:rounded-lg mt-12">
      {{#if pedi}}
      <table class="mt-5 table">
        <thead class="table-dark">
          <!--informacion en el encabezado de la tabla-->
          <tr class="">
            <th class="fs-5">Numero de Pedido</th>

            <th class="fs-5">Status del Pedido</th>

            <th class="fs-5">Detalle</th>
          </tr>
        </thead>
        <tbody>
          
          {{#each pedi}}
          <tr>
            <td class="fs-3" id="folio">{{folio}}</td>
            <td class="fs-3">{{tip_status}}</td>
            <td>
              <form class="mx-2" action="/detalle_e" method="post">
                <input type="hidden" value="{{folio}}" name="folio" id="folio">
                <button class="btn btn-dark h-10 mt-1" type="submit">
                  <h5>Detalle del Pedido</h5>
                </button>
              </form>
            </td>
            {{/each}}
          </tbody>
        </table>

          </tr>
          
          {{else}}
          <div class="empty">
            <br>
            <center><h2>No existen pedidos entregados</h2></center>
            <br>
          </div>
          {{/if}}
        </div>
    </div>
</body>
</html>